# ================================================================
# requirements.txt — DX_IA Backend


flask==3.0.3
flask-cors==4.0.1

───
httpx==0.27.0
psutil==6.0.0
PyYAML==6.0.2
pytest==8.2.2
pytest-asyncio==0.23.8
requests==2.32.3
python-dotenv==1.0.1

# TTS: Kokoro-82M (principal offline)
kokoro>=0.9.2
sounddevice==0.4.6
soundfile==0.12.1
numpy>=1.24.0

# ── TTS: Fallbacks
edge-tts==6.1.12    # Microsoft Neural (online)
pyttsx3==2.90       # Offline garantido

# ── STT: Vosk
# Instalar manualmente:
# pip install vosk
pynput==1.7.7       # Hotkey global Ctrl+Alt+M

#Automação
pyautogui==0.9.54
pywin32==308

# SETUP PÓS-INSTALAÇÃO
# ================================================================

# 1. pywin32:
# python .venv\Scripts\pywin32_postinstall.py -install

# 2. Vosk:
# pip install vosk
# Modelo PT-BR:
# https://alphacephei.com/vosk/models
# Extrair em: models\vosk-model-pt\

# 3. Ollama:
# ollama serve
# ollama pull llama3.2:3b

# 4. Setup wizard:
# python scripts/setup_wizard.py

# 5. Benchmark:
# python scripts/benchmark.py